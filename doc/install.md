# mc-fallenvaders - installation

After cloning sources, run this command at the root folder of the project:
```shell
$ mvn initialize
```

After each code update, run this command:
```shell
$ mvn clean install
```
